# 🎿 Система аренды спортивного инвентаря

![Python](https://img.shields.io/badge/Python-3.11+-blue?logo=python)
![Django](https://img.shields.io/badge/Django-5.0.1-green?logo=django)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-blue?logo=postgresql)
![Docker](https://img.shields.io/badge/Docker-Ready-2496ED?logo=docker)
![License](https://img.shields.io/badge/License-Educational-orange)

Веб-приложение для автоматизации процесса аренды спортивного оборудования (лыжи, сноуборды, коньки, санки и аксессуары).

**Учебный проект для практики УП02** "Осуществление интеграции программных модулей"  
Специальность: 09.02.07 - Информационные системы и программирование

---

## 📋 Содержание

1. [О проекте](#о-проекте)
2. [Архитектура](#архитектура)
3. [Технологический стек](#технологический-стек)
4. [Возможности системы](#возможности-системы)
5. [Быстрый старт](#быстрый-старт)
6. [Структура проекта](#структура-проекта)
7. [База данных](#база-данных)
8. [Кастомизация](#кастомизация)
9. [API и эндпоинты](#api-и-эндпоинты)
10. [Тестирование](#тестирование)
11. [Развертывание](#развертывание)
12. [Troubleshooting](#troubleshooting)
13. [Документация](#документация)
14. [Авторы](#авторы)

---

## 📖 О проекте

### Назначение

Система предназначена для:
- Автоматизации процесса аренды спортивного инвентаря
- Управления каталогом товаров
- Оформления заказов онлайн
- Расчета стоимости аренды с системой скидок
- Управления пользователями с разграничением прав доступа

### Ключевые особенности

✨ **Для клиентов:**
- Удобный каталог с поиском и фильтрацией
- Корзина с AJAX обновлением
- Автоматический калькулятор стоимости со скидками (5% от 3 дней, 10% от 7 дней)
- История заказов
- Система отзывов

✨ **Для менеджеров:**
- Управление заказами (подтверждение, изменение статуса)
- Статистика по заказам
- Расширенная админ-панель

✨ **Для администраторов:**
- Полное управление инвентарем (CRUD)
- Управление пользователями
- Назначение ролей
- Управление категориями

### Требования практики

Проект реализует все требования учебной практики УП02:

- ✅ База данных в 3 нормальной форме (3НФ)
- ✅ Диаграммы UML (IDEF1X, Use Case, Activity)
- ✅ Техническое задание (2 версии)
- ✅ Регистрация с валидацией пароля (4-16 символов, заглавные буквы, цифры, без * & { } | +)
- ✅ Авторизация с ролями (CLIENT, MANAGER, ADMIN)
- ✅ CRUD операции для всех сущностей
- ✅ Поиск, фильтрация, сортировка
- ✅ Калькулятор стоимости аренды
- ✅ 34 тест-кейса (100% успешность)
- ✅ Полная документация

---

## 🏗️ Архитектура

### Архитектурный паттерн

Проект использует **MVT (Model-View-Template)** архитектуру Django:
```
┌─────────────────────────────────────────────────────┐
│                   Browser (Client)                   │
└───────────────────────┬─────────────────────────────┘
                        │ HTTP Request
                        ▼
┌─────────────────────────────────────────────────────┐
│                   Django URLs                        │
│              (config/urls.py)                        │
└───────────────────────┬─────────────────────────────┘
                        │ Route
                        ▼
┌─────────────────────────────────────────────────────┐
│                     Views                            │
│     (accounts/views.py, inventory/views.py, etc)     │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────┐ │
│  │   Business  │  │  Validation  │  │   Utils    │ │
│  │    Logic    │  │    Logic     │  │ (Калькулятор)
│  └─────────────┘  └──────────────┘  └────────────┘ │
└──────────┬──────────────────────────────────┬───────┘
           │                                  │
           ▼                                  ▼
┌──────────────────────┐         ┌───────────────────┐
│      Models          │◄────────┤    Templates      │
│  (Database Layer)    │         │   (HTML + HTMX)   │
│                      │         └───────────────────┘
│  ┌────────────────┐  │
│  │   User         │  │
│  │   Category     │  │
│  │   Equipment    │  │
│  │   Cart         │  │
│  │   Rental       │  │
│  └────────────────┘  │
└──────────┬────────────┘
           │ ORM (Django ORM)
           ▼
┌─────────────────────────────────────────────────────┐
│             PostgreSQL Database                      │
│                  (3NF Schema)                        │
└─────────────────────────────────────────────────────┘
```

### Модульная структура

Проект разделен на 4 Django приложения:

1. **accounts** - Управление пользователями и авторизация
2. **inventory** - Каталог инвентаря
3. **rentals** - Корзина и заказы
4. **core** - Общие компоненты (главная страница, утилиты)
```
┌───────────────────────────────────────────────────────┐
│                   Django Project                       │
│                  (config/)                             │
├───────────────────────────────────────────────────────┤
│                                                        │
│  ┌─────────────┐  ┌──────────────┐  ┌─────────────┐  │
│  │  accounts   │  │  inventory   │  │  rentals    │  │
│  │             │  │              │  │             │  │
│  │ • User      │  │ • Category   │  │ • Cart      │  │
│  │ • Roles     │  │ • Equipment  │  │ • CartItem  │  │
│  │ • Auth      │  │ • Search     │  │ • Rental    │  │
│  │ • Profile   │  │ • Filters    │  │ • RentalItem│  │
│  └─────────────┘  └──────────────┘  └─────────────┘  │
│                                                        │
│                  ┌─────────────┐                       │
│                  │    core     │                       │
│                  │             │                       │
│                  │ • Index     │                       │
│                  │ • About     │                       │
│                  │ • Commands  │                       │
│                  └─────────────┘                       │
│                                                        │
└───────────────────────────────────────────────────────┘
```

### Слои приложения

**Presentation Layer (Шаблоны):**
- HTML5 + Bootstrap 5
- HTMX для AJAX
- Alpine.js для интерактивности
- AOS для анимаций

**Business Logic Layer (Views + Utils):**
- Django Class-Based Views (CBV) и Function-Based Views (FBV)
- Кастомные валидаторы
- Утилиты (калькулятор стоимости, работа с корзиной)

**Data Access Layer (Models + ORM):**
- Django ORM
- PostgreSQL драйвер (psycopg2)
- Миграции Django

---

## 🛠️ Технологический стек

### Backend

| Технология | Версия | Назначение |
|------------|--------|------------|
| **Python** | 3.11+ | Основной язык программирования |
| **Django** | 5.0.1 | Web framework (MVT) |
| **PostgreSQL** | 15+ | Реляционная СУБД |
| **psycopg2-binary** | 2.9.9 | PostgreSQL adapter для Python |
| **python-decouple** | 3.8 | Управление переменными окружения |
| **Pillow** | 10.2.0 | Обработка изображений |

### Frontend

| Технология | Версия | Назначение |
|------------|--------|------------|
| **HTML5** | - | Разметка |
| **CSS3** | - | Стилизация |
| **Bootstrap** | 5.3.2 | CSS Framework |
| **Bootstrap Icons** | 1.11 | Иконки |
| **HTMX** | 1.9.10 | AJAX без JavaScript |
| **Alpine.js** | 3.13.3 | Легкий JS framework |
| **AOS** | 2.3.1 | Анимации при скролле |

### Admin Panel

| Технология | Версия | Назначение |
|------------|--------|------------|
| **Django Unfold** | 0.18.0 | Современная админ-панель |
| **django-htmx** | 1.17.3 | HTMX интеграция |
| **django-widget-tweaks** | 1.5.0 | Кастомизация виджетов форм |

### DevOps

| Технология | Версия | Назначение |
|------------|--------|------------|
| **Docker** | - | Контейнеризация |
| **Docker Compose** | 3.8 | Оркестрация контейнеров |
| **Git** | - | Контроль версий |

---

## ⚡ Возможности системы

### Функционал по ролям

#### 👤 Гость (неавторизованный пользователь)

- Просмотр каталога товаров
- Поиск по названию, бренду, описанию
- Фильтрация по категориям, цене, состоянию
- Сортировка (цена, название, дата добавления, популярность)
- Просмотр детальной информации о товаре
- Регистрация в системе
- Авторизация

#### 🛍️ Клиент (CLIENT)

Все функции гостя +

- Добавление товаров в корзину
- Просмотр и управление корзиной
- **Калькулятор стоимости** (автоматический расчет со скидками)
- Оформление заказов
- Просмотр истории своих заказов
- Детальный просмотр заказа
- Редактирование профиля
- Написание отзывов о товарах (после завершения аренды)
- Выход из системы

#### 👔 Менеджер (MANAGER)

Все функции клиента +

- Просмотр всех заказов в системе
- Подтверждение заказов (смена статуса PENDING → CONFIRMED)
- Изменение статуса заказов (CONFIRMED → ACTIVE → COMPLETED)
- Отмена заказов
- Просмотр статистики:
  - Количество заказов по статусам
  - Выручка за период
  - Топ популярных товаров
  - Топ активных клиентов
- Доступ к расширенной админ-панели

#### 👨‍💼 Администратор (ADMIN)

Все функции менеджера +

- **Управление категориями** (CRUD):
  - Создание новых категорий
  - Редактирование названий, описаний
  - Загрузка изображений категорий
  - Удаление категорий
  
- **Управление инвентарем** (CRUD):
  - Добавление нового инвентаря
  - Редактирование параметров (цена, количество, описание)
  - Загрузка фотографий товаров
  - Изменение статуса активности
  - Удаление товаров
  
- **Управление пользователями**:
  - Просмотр всех пользователей
  - Редактирование данных пользователей
  - Назначение/изменение ролей
  - Блокировка/разблокировка пользователей
  - Удаление пользователей
  
- **Управление заказами** (полный доступ):
  - Просмотр всех заказов всех пользователей
  - Изменение любых данных заказа
  - Ручная корректировка стоимости
  - Удаление заказов

- **Модерация отзывов**:
  - Просмотр всех отзывов
  - Удаление неприемлемых отзывов

### Ключевые функции

#### 💰 Калькулятор стоимости аренды

**Формула расчета:**
```
Шаг 1: Количество дней
days = (end_date - start_date) + 1

Шаг 2: Базовая стоимость
base_price = price_per_day × quantity × days

Шаг 3: Расчет скидки
if days >= 7:
    discount = 10% от base_price
elif days >= 3:
    discount = 5% от base_price
else:
    discount = 0

Шаг 4: Итоговая стоимость
total = base_price - discount
```

**Примеры расчета:**

| Товар | Цена/день | Кол-во | Дней | База | Скидка | Итого |
|-------|-----------|--------|------|------|--------|-------|
| Лыжи | 1500₽ | 1 | 2 | 3000₽ | 0% (0₽) | **3000₽** |
| Лыжи | 1500₽ | 1 | 5 | 7500₽ | 5% (375₽) | **7125₽** |
| Лыжи | 1500₽ | 1 | 7 | 10500₽ | 10% (1050₽) | **9450₽** |
| Сноуборд | 1600₽ | 2 | 10 | 32000₽ | 10% (3200₽) | **28800₽** |

**Особенности:**
- Автоматический пересчет при изменении параметров (AJAX)
- Отображение детального расчета
- Уведомление о применении скидки
- Работает на странице добавления в корзину и в корзине

#### 🔒 Валидация пароля

**Требования к паролю:**
```python
# Длина
MIN_LENGTH = 4
MAX_LENGTH = 16

# Обязательные элементы
- Минимум 1 заглавная буква (A-Z, А-Я)
- Минимум 1 цифра (0-9)

# Запрещенные символы
FORBIDDEN_CHARS = ['*', '&', '{', '}', '|', '+']
```

**Примеры:**

✅ **Правильно:**
- `Pass123` (4 символа, заглавная P, цифры)
- `MyPass1` (7 символов, заглавная M, цифра)
- `Test2024` (8 символов, заглавная T, цифры)
- `Абв123` (кириллица тоже работает)

❌ **Неправильно:**
- `pass123` (нет заглавных букв)
- `Password` (нет цифр)
- `Pa1` (слишком короткий - меньше 4 символов)
- `ThisIsVeryLongPassword123` (слишком длинный - больше 16 символов)
- `Pass*123` (содержит запрещенный символ *)
- `Test&456` (содержит запрещенный символ &)

**Валидатор:** `accounts/validators.py` - `PasswordValidator`

#### 🛒 AJAX корзина

**Возможности:**
- Добавление товаров без перезагрузки страницы
- Изменение количества с мгновенным пересчетом
- Удаление товаров с анимацией
- Автоматическое обновление общей суммы
- Проверка доступности товара в реальном времени

**Технология:** HTMX + Django JsonResponse

#### 🔍 Поиск и фильтрация

**Поиск:**
- По названию товара
- По бренду
- По описанию
- Без учета регистра
- Частичное совпадение

**Фильтры:**
- По категории (выбор из списка)
- По диапазону цен (от-до)
- По состоянию (Отличное/Хорошее/Удовлетворительное)
- По доступности (В наличии/Нет в наличии)
- Комбинированные фильтры (все вместе)

**Сортировка:**
- По дате добавления (новые/старые)
- По цене (возрастание/убывание)
- По названию (А-Я / Я-А)
- По популярности (количество аренд)
- По рейтингу (средняя оценка)

**Реализация:** Django QuerySet API с Q-объектами

---

## 🚀 Быстрый старт

### Требования

- **Docker Desktop** (или Python 3.11+ + PostgreSQL 15+ для локального запуска)
- **Git** (для клонирования репозитория)

### Запуск в Docker (РЕКОМЕНДУЕТСЯ)

**1. Клонируй репозиторий:**
```bash
git clone <your-repo-url>
cd rent-sport-inventory-website
```

**2. Запусти Docker:**

**Windows:**
```powershell
# Двойной клик по START.bat
# или
.\START.bat
# или
docker-compose up
```

**macOS/Linux:**
```bash
# Двойной клик по START.command
# или
./START.command
# или
docker-compose up
```

**3. Открой браузер:**

http://localhost:8000/

**Готово!** База данных автоматически создана и заполнена тестовыми данными.

### Тестовые пользователи

| Логин | Пароль | Роль | Email |
|-------|--------|------|-------|
| ivan_petrov | Client123 | Клиент | ivan.petrov@example.com |
| maria_ivanova | Client123 | Клиент | maria.ivanova@example.com |
| alex_smirnov | Client123 | Клиент | alex.smirnov@example.com |
| manager | Manager123 | Менеджер | manager@sportarenda.ru |
| admin | Admin123 | Администратор | admin@sportarenda.ru |

### Локальный запуск (без Docker)

**1. Создай виртуальное окружение:**
```bash
python -m venv venv

# Windows
venv\Scripts\activate

# macOS/Linux
source venv/bin/activate
```

**2. Установи зависимости:**
```bash
pip install -r requirements.txt
```

**3. Настрой PostgreSQL:**

Создай базу данных:
```sql
CREATE DATABASE sports_rental_db;
CREATE USER sports_user WITH PASSWORD 'Sports2024!';
ALTER ROLE sports_user SET client_encoding TO 'utf8';
ALTER ROLE sports_user SET default_transaction_isolation TO 'read committed';
ALTER ROLE sports_user SET timezone TO 'UTC';
GRANT ALL PRIVILEGES ON DATABASE sports_rental_db TO sports_user;
```

**4. Создай .env файл:**
```env
DEBUG=True
SECRET_KEY=your-secret-key-here
DB_NAME=sports_rental_db
DB_USER=sports_user
DB_PASSWORD=Sports2024!
DB_HOST=localhost
DB_PORT=5432
```

**5. Примени миграции:**
```bash
python manage.py migrate
```

**6. Заполни тестовыми данными:**
```bash
python manage.py seed_data
```

**7. Запусти сервер:**
```bash
python manage.py runserver
```

**8. Открой браузер:**

http://127.0.0.1:8000/

---

## 📁 Структура проекта
```
rent-sport-inventory-website/
│
├── config/                          # Настройки Django проекта
│   ├── __init__.py
│   ├── settings.py                  # Основные настройки
│   ├── urls.py                      # Главный URL конфигуратор
│   ├── wsgi.py                      # WSGI для продакшена
│   └── asgi.py                      # ASGI для WebSockets
│
├── accounts/                        # Приложение: Пользователи
│   ├── migrations/                  # Миграции БД
│   ├── __init__.py
│   ├── admin.py                     # Админка пользователей
│   ├── apps.py
│   ├── forms.py                     # Формы регистрации/авторизации
│   ├── models.py                    # Модель User (кастомная)
│   ├── urls.py                      # URL маршруты
│   ├── validators.py                # PasswordValidator ⭐
│   └── views.py                     # Views (регистрация, авторизация, профиль)
│
├── inventory/                       # Приложение: Инвентарь
│   ├── migrations/
│   ├── __init__.py
│   ├── admin.py                     # Админка категорий и товаров
│   ├── apps.py
│   ├── models.py                    # Category, Equipment
│   ├── urls.py
│   └── views.py                     # Каталог, поиск, фильтрация ⭐
│
├── rentals/                         # Приложение: Аренда
│   ├── migrations/
│   ├── __init__.py
│   ├── admin.py                     # Админка заказов
│   ├── apps.py
│   ├── models.py                    # Cart, CartItem, Rental, RentalItem, Review
│   ├── urls.py
│   ├── utils.py                     # Калькулятор стоимости ⭐
│   └── views.py                     # Корзина, оформление, AJAX ⭐
│
├── core/                            # Приложение: Общее
│   ├── management/
│   │   └── commands/
│   │       └── seed_data.py         # Команда заполнения БД ⭐
│   ├── migrations/
│   ├── __init__.py
│   ├── admin.py
│   ├── apps.py
│   ├── models.py
│   ├── urls.py
│   └── views.py                     # Главная страница, О нас
│
├── templates/                       # HTML шаблоны
│   ├── base.html                    # Базовый шаблон
│   ├── includes/
│   │   ├── header.html              # Навигация
│   │   ├── footer.html              # Подвал
│   │   └── messages.html            # Уведомления
│   ├── accounts/
│   │   ├── login.html
│   │   ├── register.html
│   │   └── profile.html
│   ├── inventory/
│   │   ├── catalog.html             # Каталог с фильтрами
│   │   ├── equipment_detail.html
│   │   └── category.html
│   ├── rentals/
│   │   ├── add_to_cart.html         # Добавление с калькулятором ⭐
│   │   ├── cart.html                # Корзина с AJAX ⭐
│   │   ├── checkout.html
│   │   ├── my_rentals.html
│   │   └── rental_detail.html
│   └── core/
│       └── index.html               # Главная страница
│
├── static/                          # Статические файлы (пока не используется)
│   ├── css/
│   ├── js/
│   └── images/
│
├── media/                           # Загружаемые файлы (автогенерация)
│   ├── categories/
│   ├── equipment/
│   └── avatars/
│
├── docs/                            # Документация
│   ├── diagrams/
│   │   ├── 01_database_schema.md
│   │   ├── 02_use_case_diagram.md
│   │   └── 03_activity_diagram.md
│   ├── test-cases/
│   │   ├── 00_test_summary.md
│   │   ├── 01_registration_tests.md
│   │   ├── 02_authentication_tests.md
│   │   ├── 03_catalog_tests.md
│   │   └── 04_rental_tests.md
│   ├── technical-specification/
│   │   ├── TZ_v1.0_initial.md
│   │   └── TZ_v2.0_extended.md
│   └── USER_MANUAL.md
│
├── .env.example                     # Пример переменных окружения
├── .gitignore                       # Исключения для Git
├── .dockerignore                    # Исключения для Docker
├── docker-compose.yml               # Docker Compose конфигурация
├── Dockerfile                       # Образ Django
├── requirements.txt                 # Python зависимости
├── manage.py                        # Django CLI
├── START.bat                        # Запуск для Windows
├── START.ps1                        # Запуск для PowerShell
├── START.command                    # Запуск для macOS
├── ЗАПУСК.md                       # Инструкция запуска
└── README.md                        # Этот файл
```

### Ключевые файлы (⭐)

| Файл | Описание |
|------|----------|
| `accounts/validators.py` | Валидатор пароля по требованиям практики |
| `inventory/views.py` | Поиск, фильтрация, сортировка |
| `rentals/utils.py` | Калькулятор стоимости со скидками |
| `rentals/views.py` | AJAX корзина, оформление заказа |
| `core/management/commands/seed_data.py` | Автозаполнение БД |
| `templates/rentals/add_to_cart.html` | Калькулятор с AJAX |
| `templates/rentals/cart.html` | Корзина с динамическим обновлением |

---

## 🗄️ База данных

### Схема БД (IDEF1X)

База данных находится в **3 нормальной форме (3НФ)**.

#### Таблицы

**1. accounts_user** - Пользователи
```sql
id: INTEGER PRIMARY KEY
username: VARCHAR(150) UNIQUE NOT NULL
email: VARCHAR(254) UNIQUE NOT NULL
password: VARCHAR(128) NOT NULL -- bcrypt hash
first_name: VARCHAR(150) NOT NULL
last_name: VARCHAR(150) NOT NULL
role: VARCHAR(10) NOT NULL DEFAULT 'CLIENT' -- CLIENT, MANAGER, ADMIN
phone_number: VARCHAR(17)
address: TEXT
date_of_birth: DATE
avatar: VARCHAR(100)
is_active: BOOLEAN DEFAULT TRUE
created_at: TIMESTAMP NOT NULL
updated_at: TIMESTAMP NOT NULL
```

**2. inventory_category** - Категории
```sql
id: INTEGER PRIMARY KEY
name: VARCHAR(100) UNIQUE NOT NULL
slug: VARCHAR(100) UNIQUE NOT NULL
description: TEXT
image: VARCHAR(100)
is_active: BOOLEAN DEFAULT TRUE
created_at: TIMESTAMP NOT NULL
```

**3. inventory_equipment** - Инвентарь
```sql
id: INTEGER PRIMARY KEY
category_id: INTEGER FOREIGN KEY -> inventory_category(id)
name: VARCHAR(200) NOT NULL
slug: VARCHAR(200) UNIQUE NOT NULL
description: TEXT NOT NULL
image: VARCHAR(100) NOT NULL
brand: VARCHAR(100)
model: VARCHAR(100)
size: VARCHAR(50) NOT NULL
condition: VARCHAR(20) NOT NULL -- EXCELLENT, GOOD, SATISFACTORY
price_per_day: DECIMAL(10,2) NOT NULL CHECK >= 0
quantity_total: INTEGER NOT NULL CHECK >= 1
quantity_available: INTEGER NOT NULL CHECK >= 0
is_active: BOOLEAN DEFAULT TRUE
created_at: TIMESTAMP NOT NULL
updated_at: TIMESTAMP NOT NULL
```

**4. rentals_cart** - Корзина
```sql
id: INTEGER PRIMARY KEY
user_id: INTEGER UNIQUE FOREIGN KEY -> accounts_user(id) ON DELETE CASCADE
created_at: TIMESTAMP NOT NULL
updated_at: TIMESTAMP NOT NULL
```

**5. rentals_cartitem** - Позиции корзины
```sql
id: INTEGER PRIMARY KEY
cart_id: INTEGER FOREIGN KEY -> rentals_cart(id) ON DELETE CASCADE
equipment_id: INTEGER FOREIGN KEY -> inventory_equipment(id) ON DELETE CASCADE
quantity: INTEGER NOT NULL CHECK >= 1
start_date: DATE NOT NULL
end_date: DATE NOT NULL
added_at: TIMESTAMP NOT NULL
UNIQUE(cart_id, equipment_id, start_date, end_date)
```

**6. rentals_rental** - Заказы аренды
```sql
id: INTEGER PRIMARY KEY
user_id: INTEGER FOREIGN KEY -> accounts_user(id) ON DELETE CASCADE
status: VARCHAR(20) NOT NULL -- PENDING, CONFIRMED, ACTIVE, COMPLETED, CANCELLED
start_date: DATE NOT NULL
end_date: DATE NOT NULL
total_price: DECIMAL(10,2) NOT NULL CHECK >= 0
comment: TEXT
created_at: TIMESTAMP NOT NULL
updated_at: TIMESTAMP NOT NULL
confirmed_at: TIMESTAMP
confirmed_by_id: INTEGER FOREIGN KEY -> accounts_user(id) ON DELETE SET NULL
```

**7. rentals_rentalitem** - Позиции заказа
```sql
id: INTEGER PRIMARY KEY
rental_id: INTEGER FOREIGN KEY -> rentals_rental(id) ON DELETE CASCADE
equipment_id: INTEGER FOREIGN KEY -> inventory_equipment(id) ON DELETE PROTECT
quantity: INTEGER NOT NULL CHECK >= 1
price_per_day: DECIMAL(10,2) NOT NULL CHECK >= 0
days: INTEGER NOT NULL CHECK >= 1
subtotal: DECIMAL(10,2) NOT NULL CHECK >= 0
UNIQUE(rental_id, equipment_id)
```

**8. rentals_review** - Отзывы
```sql
id: INTEGER PRIMARY KEY
user_id: INTEGER FOREIGN KEY -> accounts_user(id) ON DELETE CASCADE
equipment_id: INTEGER FOREIGN KEY -> inventory_equipment(id) ON DELETE CASCADE
rental_id: INTEGER FOREIGN KEY -> rentals_rental(id) ON DELETE CASCADE
rating: INTEGER NOT NULL CHECK (1 <= rating <= 5)
comment: TEXT NOT NULL
created_at: TIMESTAMP NOT NULL
UNIQUE(user_id, equipment_id, rental_id)
```

### ER-диаграмма
```
User (1) ←──→ (1) Cart
  │                 │
  │                 │ (1:N)
  │                 ▼
  │            CartItem ←─── (N:1) ───→ Equipment
  │                                        ▲
  │ (1:N)                                  │
  ▼                                        │ (N:1)
Rental                                     │
  │                                   Category
  │ (1:N)
  ▼
RentalItem ───→ (N:1) ──→ Equipment
  
User (1) ←──→ (N) Review ←──→ (N:1) Equipment
```

### Индексы

Django автоматически создает индексы для:
- Первичных ключей (PRIMARY KEY)
- Внешних ключей (FOREIGN KEY)
- Уникальных полей (UNIQUE)

Дополнительные индексы (при необходимости):
```sql
-- Поиск по товарам
CREATE INDEX idx_equipment_name ON inventory_equipment(name);
CREATE INDEX idx_equipment_brand ON inventory_equipment(brand);

-- Фильтрация заказов
CREATE INDEX idx_rental_status ON rentals_rental(status);
CREATE INDEX idx_rental_dates ON rentals_rental(start_date, end_date);
```

### Проверка на 3НФ

**1НФ (Первая нормальная форма):**
- ✅ Все атрибуты атомарны (нет массивов, нет составных значений)
- ✅ Нет повторяющихся групп
- ✅ Каждая таблица имеет первичный ключ

**2НФ (Вторая нормальная форма):**
- ✅ Выполнена 1НФ
- ✅ Все неключевые атрибуты полностью зависят от первичного ключа
- ✅ Нет частичных зависимостей (все таблицы имеют простые первичные ключи)

**3НФ (Третья нормальная форма):**
- ✅ Выполнена 2НФ
- ✅ Отсутствуют транзитивные зависимости
- ✅ Все неключевые атрибуты зависят только от первичного ключа

**Примеры нормализации:**

❌ **НЕ нормализовано:**
```sql
rental: id, user_name, user_email, equipment_name, equipment_price, ...
-- Транзитивная зависимость: user_email зависит от user_name
```

✅ **Нормализовано:**
```sql
user: id, name, email
rental: id, user_id (FK), equipment_id (FK), ...
equipment: id, name, price
```

---

## 🎨 Кастомизация

### 1. Смена базы данных (PostgreSQL → MySQL)

**Шаг 1:** Установи MySQL драйвер

Замени в `requirements.txt`:
```txt
# Было
psycopg2-binary==2.9.9

# Стало
mysqlclient==2.2.0
```

**Шаг 2:** Обнови `config/settings.py`
```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',  # Изменено
        'NAME': config('DB_NAME', default='sports_rental_db'),
        'USER': config('DB_USER', default='sports_user'),
        'PASSWORD': config('DB_PASSWORD', default='Sports2024!'),
        'HOST': config('DB_HOST', default='localhost'),
        'PORT': config('DB_PORT', default='3306'),  # MySQL порт
        'OPTIONS': {
            'charset': 'utf8mb4',
            'init_command': "SET sql_mode='STRICT_TRANS_TABLES'",
        }
    }
}
```

**Шаг 3:** Создай MySQL базу данных
```sql
CREATE DATABASE sports_rental_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'sports_user'@'localhost' IDENTIFIED BY 'Sports2024!';
GRANT ALL PRIVILEGES ON sports_rental_db.* TO 'sports_user'@'localhost';
FLUSH PRIVILEGES;
```

**Шаг 4:** Обнови `.env`
```env
DB_HOST=localhost
DB_PORT=3306
```

**Шаг 5:** Обнови `docker-compose.yml` (если используешь Docker)
```yaml
db:
  image: mysql:8.0
  container_name: sports_rental_db
  environment:
    MYSQL_DATABASE: sports_rental_db
    MYSQL_USER: sports_user
    MYSQL_PASSWORD: Sports2024!
    MYSQL_ROOT_PASSWORD: rootpassword
  ports:
    - "3306:3306"
  command: --default-authentication-plugin=mysql_native_password
```

**Шаг 6:** Примени миграции
```bash
python manage.py migrate
python manage.py seed_data
```

### 2. Смена на SQLite (для тестирования)

В `config/settings.py`:
```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}
```

Удали `psycopg2-binary` из `requirements.txt`.

### 3. Генерация SECRET_KEY

**Способ 1: Python**
```bash
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

**Способ 2: Django shell**
```bash
python manage.py shell
```
```python
from django.core.management.utils import get_random_secret_key
print(get_random_secret_key())
```

**Способ 3: Online генератор**

https://djecrety.ir/

**Добавь в `.env`:**
```env
SECRET_KEY=your-generated-secret-key-here-50-characters
```

### 4. Кастомизация шаблонов

#### Изменение цветовой схемы

Открой `templates/base.html` и найди секцию `<style>`:
```css
<style>
    :root {
        /* Основные цвета */
        --primary-color: #6366f1;      /* Индиго - измени на свой */
        --secondary-color: #8b5cf6;    /* Фиолетовый */
        --success-color: #10b981;      /* Зеленый */
        --danger-color: #ef4444;       /* Красный */
        --warning-color: #f59e0b;      /* Оранжевый */
        --info-color: #3b82f6;         /* Синий */
        
        /* Градиенты */
        --gradient-primary: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
    }
</style>
```

**Пример: Смена на красно-оранжевую тему**
```css
:root {
    --primary-color: #ef4444;      /* Красный */
    --secondary-color: #f97316;    /* Оранжевый */
    --gradient-primary: linear-gradient(135deg, #ef4444 0%, #f97316 100%);
}
```

#### Создание собственных шаблонов

**Структура:**
```
templates/
└── your_app/
    └── your_template.html
```

**Шаблон должен наследовать base.html:**
```django
{% extends 'base.html' %}

{% block title %}Название страницы{% endblock %}

{% block content %}
<div class="container mt-5">
    <h1>Ваш контент</h1>
    <!-- Ваш HTML -->
</div>
{% endblock %}

{% block extra_js %}
<script>
    // Ваш JavaScript
</script>
{% endblock %}
```

**Доступные блоки:**

- `{% block title %}` - Заголовок страницы
- `{% block extra_css %}` - Дополнительные стили
- `{% block content %}` - Основной контент
- `{% block extra_js %}` - Дополнительный JavaScript

#### Добавление своих CSS стилей

**Вариант 1: Inline в base.html**
```html
<style>
.my-custom-class {
    color: #ff0000;
}
</style>
```

**Вариант 2: Отдельный файл**

Создай `static/css/custom.css`:
```css
/* static/css/custom.css */
.my-custom-class {
    color: #ff0000;
}
```

Подключи в `base.html`:
```django
{% load static %}
<link rel="stylesheet" href="{% static 'css/custom.css' %}">
```

Не забудь собрать статику:
```bash
python manage.py collectstatic
```

#### Изменение логотипа

В `templates/includes/header.html` найди:
```html
<a class="navbar-brand fw-bold" href="{% url 'core:index' %}">
    <i class="bi bi-snow"></i> СпортАренда
</a>
```

Замени на свой логотип:
```html
<a class="navbar-brand" href="{% url 'core:index' %}">
    <img src="{% static 'images/logo.png' %}" alt="Logo" height="40">
</a>
```

### 5. Добавление новых моделей

**Пример: Добавление модели "Отзывы" (уже есть, но для примера)**

**Шаг 1:** Создай модель в `rentals/models.py`
```python
class Review(models.Model):
    user = models.ForeignKey(User, on_delete=models.CASCADE)
    equipment = models.ForeignKey(Equipment, on_delete=models.CASCADE)
    rating = models.IntegerField(
        validators=[MinValueValidator(1), MaxValueValidator(5)]
    )
    comment = models.TextField()
    created_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        verbose_name = 'Отзыв'
        verbose_name_plural = 'Отзывы'
        unique_together = ['user', 'equipment']
    
    def __str__(self):
        return f'Отзыв от {self.user.username} на {self.equipment.name}'
```

**Шаг 2:** Зарегистрируй в админке `rentals/admin.py`
```python
from .models import Review

@admin.register(Review)
class ReviewAdmin(admin.ModelAdmin):
    list_display = ['user', 'equipment', 'rating', 'created_at']
    list_filter = ['rating', 'created_at']
    search_fields = ['user__username', 'equipment__name']
```

**Шаг 3:** Создай и примени миграции
```bash
python manage.py makemigrations
python manage.py migrate
```

**Шаг 4:** Создай view
```python
# rentals/views.py
from .models import Review

@login_required
def add_review_view(request, equipment_id):
    equipment = get_object_or_404(Equipment, id=equipment_id)
    
    if request.method == 'POST':
        rating = request.POST.get('rating')
        comment = request.POST.get('comment')
        
        Review.objects.create(
            user=request.user,
            equipment=equipment,
            rating=rating,
            comment=comment
        )
        
        messages.success(request, 'Отзыв добавлен!')
        return redirect('inventory:equipment_detail', slug=equipment.slug)
    
    return render(request, 'rentals/add_review.html', {'equipment': equipment})
```

**Шаг 5:** Добавь URL
```python
# rentals/urls.py
path('add-review/<int:equipment_id>/', views.add_review_view, name='add_review'),
```

### 6. Изменение логики калькулятора

Открой `rentals/utils.py`:
```python
def calculate_rental_cost(equipment, quantity, start_date, end_date):
    """
    Калькулятор стоимости аренды
    """
    days = (end_date - start_date).days + 1
    total = equipment.price_per_day * quantity * days
    
    discount = Decimal('0')
    
    # ИЗМЕНИ ЗДЕСЬ: Твои условия скидок
    if days >= 7:
        discount = total * Decimal('0.10')  # 10% от 7 дней
    elif days >= 3:
        discount = total * Decimal('0.05')  # 5% от 3 дней
    
    # Можно добавить другие условия:
    # if quantity >= 5:
    #     discount += total * Decimal('0.05')  # Скидка за количество
    
    final_price = total - discount
    
    return {
        'base_price': equipment.price_per_day,
        'quantity': quantity,
        'days': days,
        'subtotal': total,
        'discount': discount,
        'discount_percent': (discount / total * 100) if total > 0 else 0,
        'total': final_price,
    }
```

**Пример: Скидка за количество**
```python
# Скидка 5% при аренде 5+ единиц
if quantity >= 5:
    discount += total * Decimal('0.05')

# Скидка 10% при аренде 10+ единиц
if quantity >= 10:
    discount += total * Decimal('0.10')
```

**Пример: Сезонная скидка**
```python
from datetime import date

# Скидка 15% в межсезонье (апрель-октябрь)
if 4 <= start_date.month <= 10:
    discount += total * Decimal('0.15')
```

### 7. Добавление email уведомлений

**Шаг 1:** Настрой email в `config/settings.py`
```python
# Email настройки
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'  # или ваш SMTP сервер
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = config('EMAIL_HOST_USER', default='your-email@gmail.com')
EMAIL_HOST_PASSWORD = config('EMAIL_HOST_PASSWORD', default='your-app-password')
DEFAULT_FROM_EMAIL = EMAIL_HOST_USER
```

**Шаг 2:** Добавь в `.env`
```env
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-specific-password
```

**Шаг 3:** Отправка email при создании заказа
```python
# rentals/views.py
from django.core.mail import send_mail

def checkout_view(request):
    # ... создание заказа ...
    
    # Отправка email
    send_mail(
        subject=f'Заказ №{rental.id} создан',
        message=f'Здравствуйте, {request.user.get_full_name()}!\n\n'
                f'Ваш заказ №{rental.id} успешно создан.\n'
                f'Сумма: {rental.total_price} ₽\n\n'
                f'Спасибо за заказ!',
        from_email=settings.DEFAULT_FROM_EMAIL,
        recipient_list=[request.user.email],
        fail_silently=False,
    )
```

### 8. Изменение количества товаров в seed_data

Открой `core/management/commands/seed_data.py`:
```python
# Найди секцию создания категорий
categories_data = [
    {
        'name': 'Горные лыжи',
        'description': 'Лыжи для катания по горным склонам',
        # ... остальное
    },
    # ДОБАВЬ СВОИ КАТЕГОРИИ ЗДЕСЬ
    {
        'name': 'Ваша новая категория',
        'description': 'Описание',
        # ...
    }
]

# Найди секцию создания инвентаря
equipment_data = [
    {
        'name': 'Горные лыжи Head Kore 93',
        'category': categories[0],
        'description': 'Универсальные лыжи...',
        'price_per_day': 1500,
        'quantity_total': 5,  # ИЗМЕНИ КОЛИЧЕСТВО
        # ...
    },
    # ДОБАВЬ СВОИ ТОВАРЫ
]
```

Затем:
```bash
# Удали старые данные
docker-compose exec web python manage.py flush

# Заполни заново
docker-compose exec web python manage.py seed_data
```

### 9. Добавление новой страницы

**Шаг 1:** Создай view
```python
# core/views.py
def about_view(request):
    return render(request, 'core/about.html')
```

**Шаг 2:** Создай URL
```python
# core/urls.py
path('about/', views.about_view, name='about'),
```

**Шаг 3:** Создай шаблон
```django
<!-- templates/core/about.html -->
{% extends 'base.html' %}

{% block title %}О нас{% endblock %}

{% block content %}
<div class="container mt-5">
    <h1>О нас</h1>
    <p>Информация о компании...</p>
</div>
{% endblock %}
```

**Шаг 4:** Добавь ссылку в навигацию
```django
<!-- templates/includes/header.html -->
<li class="nav-item">
    <a class="nav-link" href="{% url 'core:about' %}">О нас</a>
</li>
```

### 10. Настройка пагинации

В `inventory/views.py`:
```python
from django.core.paginator import Paginator

def catalog_view(request):
    equipment_list = Equipment.objects.filter(is_active=True)
    
    # ... фильтры и сортировка ...
    
    # Пагинация
    paginator = Paginator(equipment_list, 12)  # 12 товаров на страницу
    page_number = request.GET.get('page')
    equipment_list = paginator.get_page(page_number)
    
    context = {
        'equipment_list': equipment_list,
        # ...
    }
    
    return render(request, 'inventory/catalog.html', context)
```

В шаблоне `templates/inventory/catalog.html`:
```django
<!-- Пагинация -->
{% if equipment_list.has_other_pages %}
<nav>
    <ul class="pagination justify-content-center">
        {% if equipment_list.has_previous %}
            <li class="page-item">
                <a class="page-link" href="?page={{ equipment_list.previous_page_number }}">Предыдущая</a>
            </li>
        {% endif %}
        
        {% for num in equipment_list.paginator.page_range %}
            <li class="page-item {% if equipment_list.number == num %}active{% endif %}">
                <a class="page-link" href="?page={{ num }}">{{ num }}</a>
            </li>
        {% endfor %}
        
        {% if equipment_list.has_next %}
            <li class="page-item">
                <a class="page-link" href="?page={{ equipment_list.next_page_number }}">Следующая</a>
            </li>
        {% endif %}
    </ul>
</nav>
{% endif %}
```

---

## 🔌 API и эндпоинты

### Основные URL маршруты

| URL | Метод | Описание | Авторизация |
|-----|-------|----------|-------------|
| `/` | GET | Главная страница | Нет |
| `/accounts/register/` | GET, POST | Регистрация | Нет |
| `/accounts/login/` | GET, POST | Вход | Нет |
| `/accounts/logout/` | GET | Выход | Да |
| `/accounts/profile/` | GET, POST | Профиль | Да |
| `/catalog/` | GET | Каталог товаров | Нет |
| `/catalog/<slug>/` | GET | Детали товара | Нет |
| `/rentals/cart/` | GET | Корзина | Да |
| `/rentals/add-to-cart/<id>/` | GET, POST | Добавить в корзину | Да |
| `/rentals/checkout/` | GET, POST | Оформление заказа | Да |
| `/rentals/my-rentals/` | GET | Мои заказы | Да |
| `/admin/` | GET | Админ-панель | Да (staff) |

### AJAX эндпоинты

| URL | Метод | Описание | Response |
|-----|-------|----------|----------|
| `/rentals/calculate-cost/` | POST | Калькулятор стоимости | JSON |
| `/rentals/update-cart-item/<id>/` | POST | Обновление корзины | JSON |

**Пример запроса к калькулятору:**
```javascript
const formData = new FormData();
formData.append('csrfmiddlewaretoken', csrfToken);
formData.append('equipment_id', 1);
formData.append('quantity', 2);
formData.append('start_date', '2026-01-20');
formData.append('end_date', '2026-01-27');

fetch('/rentals/calculate-cost/', {
    method: 'POST',
    body: formData
})
.then(response => response.json())
.then(data => {
    console.log(data);
    // {
    //   "success": true,
    //   "data": {
    //     "base_price": 1500.00,
    //     "quantity": 2,
    //     "days": 7,
    //     "subtotal": 21000.00,
    //     "discount": 2100.00,
    //     "discount_percent": 10.0,
    //     "total": 18900.00
    //   }
    // }
});
```

---

## 🧪 Тестирование

### Запуск тестов
```bash
# Все тесты
python manage.py test

# Тесты конкретного приложения
python manage.py test accounts
python manage.py test inventory
python manage.py test rentals

# С подробным выводом
python manage.py test --verbosity=2
```

### Тест-кейсы

Проект включает 34 тест-кейса:

- **Регистрация:** 10 тестов
- **Авторизация:** 6 тестов
- **Каталог:** 8 тестов
- **Корзина и аренда:** 10 тестов

**Успешность:** 100% (34/34 PASSED)

Подробности: `docs/test-cases/`

### Создание своих тестов

Создай файл `tests.py` в приложении:
```python
# accounts/tests.py
from django.test import TestCase
from django.contrib.auth import get_user_model

User = get_user_model()

class UserModelTest(TestCase):
    def setUp(self):
        self.user = User.objects.create_user(
            username='testuser',
            email='test@example.com',
            password='Test123'
        )
    
    def test_user_creation(self):
        """Тест создания пользователя"""
        self.assertEqual(self.user.username, 'testuser')
        self.assertEqual(self.user.email, 'test@example.com')
        self.assertTrue(self.user.check_password('Test123'))
    
    def test_user_role_default(self):
        """Тест роли по умолчанию"""
        self.assertEqual(self.user.role, User.Role.CLIENT)
```

---

## 🚀 Развертывание

### Docker (Рекомендуется)

**Для разработки:**
```bash
docker-compose up
```

**Для продакшена:**

1. Создай `.env` с продакшен настройками:
```env
DEBUG=False
SECRET_KEY=super-secure-production-key-50-chars-minimum
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
DB_NAME=sports_rental_db
DB_USER=sports_user
DB_PASSWORD=SecurePassword123!
DB_HOST=db
DB_PORT=5432
```

2. Используй Gunicorn:
```yaml
# docker-compose.prod.yml
services:
  web:
    command: gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 3
    environment:
      - DEBUG=False
```

3. Добавь Nginx для статики

### Heroku

**1. Создай `Procfile`:**
```
web: gunicorn config.wsgi --log-file -
release: python manage.py migrate
```

**2. Создай `runtime.txt`:**
```
python-3.11.7
```

**3. Обнови `settings.py` для Heroku:**
```python
import dj_database_url

DATABASES = {
    'default': dj_database_url.config(
        default=config('DATABASE_URL', default='postgresql://localhost/sports_rental_db')
    )
}

# Static files
STATIC_ROOT = BASE_DIR / 'staticfiles'
STATICFILES_STORAGE = 'whitenoise.storage.CompressedManifestStaticFilesStorage'

# Heroku
ALLOWED_HOSTS = config('ALLOWED_HOSTS', default='localhost').split(',')
```

**4. Деплой:**
```bash
heroku login
heroku create your-app-name
heroku addons:create heroku-postgresql:hobby-dev
git push heroku main
heroku run python manage.py migrate
heroku run python manage.py seed_data
heroku run python manage.py createsuperuser
```

### VPS (Ubuntu)

**1. Обнови систему:**
```bash
sudo apt update && sudo apt upgrade -y
```

**2. Установи зависимости:**
```bash
sudo apt install python3-pip python3-venv postgresql nginx -y
```

**3. Настрой PostgreSQL:**
```bash
sudo -u postgres psql
CREATE DATABASE sports_rental_db;
CREATE USER sports_user WITH PASSWORD 'Sports2024!';
GRANT ALL PRIVILEGES ON DATABASE sports_rental_db TO sports_user;
\q
```

**4. Клонируй проект:**
```bash
cd /var/www/
git clone <your-repo-url>
cd rent-sport-inventory-website
```

**5. Создай виртуальное окружение:**
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
pip install gunicorn
```

**6. Создай `.env`:**
```env
DEBUG=False
SECRET_KEY=your-production-secret-key
DB_HOST=localhost
```

**7. Примени миграции:**
```bash
python manage.py migrate
python manage.py collectstatic
python manage.py seed_data
```

**8. Настрой Gunicorn:**
```bash
sudo nano /etc/systemd/system/gunicorn.service
```
```ini
[Unit]
Description=gunicorn daemon
After=network.target

[Service]
User=www-data
Group=www-data
WorkingDirectory=/var/www/rent-sport-inventory-website
ExecStart=/var/www/rent-sport-inventory-website/venv/bin/gunicorn \
          --workers 3 \
          --bind unix:/var/www/rent-sport-inventory-website/gunicorn.sock \
          config.wsgi:application

[Install]
WantedBy=multi-user.target
```
```bash
sudo systemctl start gunicorn
sudo systemctl enable gunicorn
```

**9. Настрой Nginx:**
```bash
sudo nano /etc/nginx/sites-available/sports_rental
```
```nginx
server {
    listen 80;
    server_name yourdomain.com;

    location = /favicon.ico { access_log off; log_not_found off; }
    
    location /static/ {
        root /var/www/rent-sport-inventory-website;
    }
    
    location /media/ {
        root /var/www/rent-sport-inventory-website;
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/var/www/rent-sport-inventory-website/gunicorn.sock;
    }
}
```
```bash
sudo ln -s /etc/nginx/sites-available/sports_rental /etc/nginx/sites-enabled
sudo nginx -t
sudo systemctl restart nginx
```

**10. Настрой SSL (Let's Encrypt):**
```bash
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d yourdomain.com
```

---

## 🐛 Troubleshooting

### Проблема: "Порт 8000 занят"

**Решение:**
```bash
# Windows
netstat -ano | findstr :8000
taskkill /PID <PID> /F

# macOS/Linux
lsof -i :8000
kill -9 <PID>
```

Или измени порт в `docker-compose.yml`:
```yaml
ports:
  - "8001:8000"
```

### Проблема: "CSRF token missing"

**Решение:**

Убедись что в форме есть:
```django
<form method="post">
    {% csrf_token %}
    <!-- fields -->
</form>
```

### Проблема: "Static files not found"

**Решение:**
```bash
python manage.py collectstatic --clear
python manage.py collectstatic
```

Проверь `settings.py`:
```python
STATIC_URL = '/static/'
STATIC_ROOT = BASE_DIR / 'staticfiles'
```

### Проблема: "Database connection error"

**Решение:**

1. Проверь что PostgreSQL запущен:
```bash
# Windows
services.msc -> PostgreSQL

# macOS
brew services list

# Linux
sudo systemctl status postgresql
```

2. Проверь `.env`:
```env
DB_HOST=localhost  # или 'db' в Docker
DB_PORT=5432
DB_NAME=sports_rental_db
DB_USER=sports_user
DB_PASSWORD=Sports2024!
```

3. Проверь подключение:
```bash
psql -h localhost -U sports_user -d sports_rental_db
```

### Проблема: "Migration conflicts"

**Решение:**
```bash
# Откатить все миграции
python manage.py migrate <app_name> zero

# Удалить файлы миграций
rm -rf */migrations/0*.py

# Создать заново
python manage.py makemigrations
python manage.py migrate
```

### Проблема: "Docker не запускается"

**Решение:**

1. Убедись что Docker Desktop запущен
2. Перезапусти Docker Desktop
3. Проверь логи:
```bash
docker-compose logs
```

4. Пересобери контейнеры:
```bash
docker-compose down -v
docker-compose up --build
```

---

## 📚 Документация

### Структура документации
```
docs/
├── diagrams/
│   ├── 01_database_schema.md      # IDEF1X схема БД
│   ├── 02_use_case_diagram.md     # Use Case диаграмма
│   └── 03_activity_diagram.md     # Activity диаграмма
├── test-cases/
│   ├── 00_test_summary.md         # Сводка тестирования
│   ├── 01_registration_tests.md   # Тесты регистрации
│   ├── 02_authentication_tests.md # Тесты авторизации
│   ├── 03_catalog_tests.md        # Тесты каталога
│   └── 04_rental_tests.md         # Тесты аренды
├── technical-specification/
│   ├── TZ_v1.0_initial.md         # ТЗ версия 1.0
│   └── TZ_v2.0_extended.md        # ТЗ версия 2.0
├── USER_MANUAL.md                 # Руководство пользователя
├── PRESENTATION.md                # Презентация проекта
└── ЗАПУСК.md                      # Инструкция запуска
```

### Дополнительные ресурсы

- [Django Documentation](https://docs.djangoproject.com/en/5.0/)
- [Bootstrap Documentation](https://getbootstrap.com/docs/5.3/)
- [HTMX Documentation](https://htmx.org/docs/)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)

---

## 👥 Авторы

**Разработчик:** [Ваше ФИО]  
**Группа:** ТИП-[номер]  
**Специальность:** 09.02.07 - Информационные системы и программирование  
**Учебное заведение:** МГТУ им. Н.Э. Баумана - Московский техникум космического приборостроения  

**Руководитель практики:** [ФИО преподавателя]

---

## 📄 Лицензия

Этот проект создан в образовательных целях для учебной практики УП02.

---
