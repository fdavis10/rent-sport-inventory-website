# 🚀 Как запустить проект

## Что нужно установить

**Только Docker Desktop:**
- Windows: https://www.docker.com/products/docker-desktop/
- macOS: https://www.docker.com/products/docker-desktop/

## Как запустить

### Способ 1: Двойной клик (САМЫЙ ПРОСТОЙ)

**Windows:**
1. Двойной клик по файлу `START.bat`
2. Подожди 1-2 минуты
3. Открой в браузере: http://localhost:8000/

**macOS:**
1. Двойной клик по файлу `START.command`
2. Подожди 1-2 минуты  
3. Открой в браузере: http://localhost:8000/

### Способ 2: Через терминал

Открой терминал в папке проекта и выполни:
```bash
docker-compose up
```

Первый раз займёт 2-3 минуты (скачивает всё нужное).  
Дальше будет запускаться за 30 секунд.

## Вход в систему

Открой http://localhost:8000/ и войди:

| Логин | Пароль | Роль |
|-------|--------|------|
| ivan_petrov | Client123 | Клиент |
| manager | Manager123 | Менеджер |
| admin | Admin123 | Админ |

Или зарегистрируйся: http://localhost:8000/accounts/register/

## Как остановить

Нажми **Ctrl+C** в терминале (или **Cmd+C** на Mac)

Или просто закрой окно терминала.

## Если что-то сломалось

### Вариант 1: Перезапуск
```bash
docker-compose down
docker-compose up
```

### Вариант 2: Полная переустановка
```bash
docker-compose down -v
docker-compose up --build
```

Это удалит всё и создаст заново с нуля.

## Частые проблемы

**"Порт 8000 занят"**
- Найди что использует порт:
  - Windows: `netstat -ano | findstr :8000`
  - Mac: `lsof -i :8000`
- Или измени порт в `docker-compose.yml`:
```yaml
  ports:
    - "8001:8000"  
```

**"Docker не запускается"**
- Проверь что Docker Desktop запущен
- Перезапусти Docker Desktop

**"Долго грузится"**
- Первый запуск всегда долгий (скачивает образы)
- Следующие запуски будут быстрее

## Что дальше?

После запуска можешь:
- 🛍️ Просматривать каталог товаров
- 🛒 Добавлять в корзину
- 📦 Оформлять заказы
- 💰 Калькулятор стоимости работает автоматически
- ⭐ Оставлять отзывы